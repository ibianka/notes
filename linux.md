# SHELL NOTES

list directory:

```bash
ls
```

change directory:

```bash
cd
```

# Linux notes

## Update ubuntu

update repositories list index:

```bash
sudo apt-get update
```

update the entire linux distribution:
```bash
sudo apt-get dist-upgrade
```